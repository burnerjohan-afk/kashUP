# URL de l'API (IP du PC quand l'app tourne sur téléphone / autre machine)
# Exemple : EXPO_PUBLIC_API_URL=http://192.168.1.13:4000
# Sans /api/v1 — ajouté automatiquement.
# Si non défini : l'app utilise l'IP détectée par Expo (hostUri).
EXPO_PUBLIC_API_URL=http://192.168.1.13:4000

